# Copy this file to .env for Node scripts/CLI (do NOT commit real values)

# Supabase project URL (https://<project-ref>.supabase.co)
SUPABASE_URL=https://your-project-ref.supabase.co

# Public anon key (publishable). Used by health/storage checks.
SUPABASE_ANON_KEY=your_anon_public_key

# Service role key (HIGHLY SENSITIVE). Required by seed scripts that upsert data.
# Only use locally or in secure CI secrets, never expose to client code.
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Storage bucket name for uploads (used by storage health check script)
REPORT_BUCKET=report-photos
