# Copy this file to .env.local and fill in your values.
# IMPORTANT: Do NOT commit your real keys. This file is for local development only.
# Vite env vars must be prefixed with VITE_. These are exposed to the browser.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_anon_public_key

# Optional: Mapbox (used by components/report/report-form.tsx)
VITE_MAPBOX_TOKEN=pk.XXXXXXXXXXXXXXXX

# Optional: comma-separated admin emails allowlist (fallback if DB role check fails)
# Example: admin1@example.com,admin2@example.com
VITE_ADMIN_EMAILS=admin@example.com
